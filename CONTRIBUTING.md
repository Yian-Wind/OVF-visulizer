# 贡献说明

请将可复现的前端逻辑集中在 ovf_universal_reader.html。历史实现放在 archive/，除非为了记录迁移，不要把旧版本重新作为主入口。

提交前检查：

1. 用文本 OVF 和 Binary 4/8 OVF 各测试一次。
2. 分别测试温度模式、磁滞模式、自动识别和错误文件提示。
3. 点选曲线数据点，确认磁矩分布与选中状态同步。
4. 检查浏览器控制台无新增错误，并确认文件只在本地读取。
